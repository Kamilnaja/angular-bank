import {RestrictCurrencyDirective} from './restrict-currency.directive';

describe('RestrictCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
